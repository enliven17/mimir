[profile.default]
src = "contracts"
test = "contracts/test"
out = "forge-out"
cache_path = "forge-cache"
libs = []
solc = "0.8.28"
# The claim struct and the 17-argument createClaim put the legacy pipeline over
# its stack limit, so the contract is compiled the same way it is deployed.
via_ir = true
optimizer = true
optimizer_runs = 200

[fmt]
line_length = 100
